import { d3Call } from 'dummy/helpers/d3-call';
import { module, test } from 'qunit';

module('Unit | Helper | d3 call');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Call([42]);
  assert.ok(result);
});
