import { d3On } from 'dummy/helpers/d3-on';
import { module, test } from 'qunit';

module('Unit | Helper | d3 on');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3On(['mousedown']);
  assert.ok(result);
});
