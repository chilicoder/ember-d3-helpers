
import { d3Format } from 'dummy/helpers/d3-format';
import { module, test } from 'qunit';

module('Unit | Helper | d3 format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Format([42]);
  assert.ok(result);
});

