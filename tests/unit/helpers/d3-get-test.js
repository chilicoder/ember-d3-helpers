import { d3Get } from 'dummy/helpers/d3-get';
import { module, test } from 'qunit';

module('Unit | Helper | d3 get');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Get([42]);
  assert.ok(result);
});
