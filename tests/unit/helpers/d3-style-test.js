import { d3Style } from 'dummy/helpers/d3-style';
import { module, test } from 'qunit';

module('Unit | Helper | d3 style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Style([42]);
  assert.ok(result);
});
