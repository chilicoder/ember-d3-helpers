import { d3Text } from 'dummy/helpers/d3-text';
import { module, test } from 'qunit';

module('Unit | Helper | d3 text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Text([42]);
  assert.ok(result);
});
